# TransferMap GT Scraper Configuration

# Entry URL for Georgia Tech Transfer Equivalency system
BASE_URL=https://oscar.gatech.edu/pls/bprod/wwsktrna.P_find_location

# State to scrape (must match dropdown option exactly)
STATE_NAME=Georgia

# Academic level (must match dropdown option exactly)
LEVEL=Undergraduate

# Semester/term (must match dropdown option exactly)
SEMESTER=Fall 2025

# Rate limiting: requests per minute (be polite to the server)
REQUESTS_PER_MINUTE=8

# Retry configuration
RETRY_MAX=3
RETRY_BACKOFF_SECONDS=2

# User agent string (customize with your contact info)
USER_AGENT=TransferMapGT/0.1 (student project; contact: myemail@example.com)

# Database path relative to scraper directory
DB_PATH=../data/transfermap.db

# Optional filters for testing (comment out to scrape all)
# SCHOOL_NAME_FILTER=Abraham Baldwin
# SUBJECT_PREFIX_FILTER=BIOL